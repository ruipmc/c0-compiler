/*
 * Example 4 - Compute and print a table of the
 * first 20 Fibonnacci numbers.
 * Pedro Vasconcelos, 2023.
 */
 
int main() {
   int fib[20];
   int i;

   fib[0] = 0;
   fib[1] = 1;
   
   for(i=2; i<20; i++)
      fib[i] = fib[i-1] + fib[i-2];

   for(i=0; i<20; i++) {
      print_int(fib[i]);	
   }				
}

